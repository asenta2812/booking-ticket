const initialState = {

}

export const BookingTicketReducer =  (state = initialState, action) => {
    // switch (type) {

    // case typeName:
    //     return { ...state, ...payload }

    // default:
    //     return state
    // }
    return {...state}
}
